# Set to 'test' to enable the /api/todos/reset endpoint (used by E2E tests only)
APP_ENV=

# Secret token required by DELETE /api/todos/reset when APP_ENV=test.
# Must match the x-reset-secret header sent by E2E tests.
# Playwright defaults this to 'e2e-reset-secret' if unset.
RESET_SECRET=
